# Production release: Astra and homepage polish

Deployed September 7, 2026 (September 8 UTC) to https://useorune.com.

- Source/image/template: `defaa1d`, branch `codex/astra-homepage-polish`.
- Merges `origin/main` at `2ad5492`, preserving the production `f297081` pipeline, narration improvements, parallel rendering and homepage visuals.
- Cloud Build: `160d48da-efd3-4157-ae01-af63606877be` — succeeded, including type checks, unit tests, production build and dependency audit.
- API: `lesson-studio-staging-api-00089-8b5`, 100% traffic.
- Dispatcher: `lesson-studio-staging-dispatcher-00083-4pv`, 100% traffic.
- Both services use the immutable app image and `E2B_TEMPLATE_VERSION=defaa1d`.
- Migration `lesson-studio-staging-migrate-2m6g7` succeeded before either service update.
- Terraform plan after deployment: no changes. Ignored local `useorune.auto.tfvars` records the matching image/template and existing ElevenLabs secret ID. No database, network, IAM, billing, or capacity changes were needed.

## Active routing and costs

Script: GPT-5.4 / low. Faster: GPT-5.6 Terra / low. Balanced: GPT-6 Astra / medium. Try harder: GPT-6 Astra / high. The production API overrides sandbox model/reasoning choices and disallows priority processing. `ORUNE_CODE_MODEL_BALANCED` is independent of the Faster override.

Existing production estimated-spend admission thresholds remain $3.50 per normal job and $7 for Try harder. These are not hard invoice caps: an in-flight request can overshoot and rendering/narration are separate. The complete-scene output-token limit stays 32,000. Subscription prices and credit allowances are unchanged. Stripe stays in **live** mode.

## Verification

- Local unit suite: 46 passed, 4 database-dependent tests skipped.
- Local desktop/mobile public-route suite: 14 passed, 4 intentionally inapplicable cases skipped.
- Tiny provider smoke `lesson-studio-staging-model-smoke-defaa1d-wptwf`: the actual production secret accessed Astra with both medium and high reasoning and structured JSON output. The key remained in Secret Manager.
- E2B build `lesson-studio-staging-e2b-release-defaa1d-flkjz` and smoke `lesson-studio-staging-e2b-smoke-defaa1d-dkh8c`: succeeded. Smoke includes Node 22, Python/Manim import, fonts, pipeline dependencies, workspace I/O, blocked arbitrary internet access and sandbox termination.
- Balanced Astra pipeline eval `lesson-studio-staging-eval-defaa1d-f9cxp`: generated a silent 23.7-second circle-to-sine-wave explanation, including frame review, in 201.8 seconds. Contact sheet inspected. Private artifacts: `gs://educationalvideo-506219-lesson-studio-staging-artifacts/eval/astra-defaa1d/`. This is one functional example, not a quality or cost benchmark.
- Live browser: homepage, sample video metadata loading, mobile menu, pricing cards and sign-in passed with no JavaScript runtime errors.
- Live disposable free account: signup, administrative verification, login, generation, and private video download succeeded. Job `812d5c2b-b5b4-499e-9be2-acd95a93aa00` completed in 41.4 seconds. This tests Faster, not the paid Astra entitlement path. The test account was deleted afterward.
- Public health/readiness endpoints return 200; direct API `run.app` origin returns 404 as intended.

No live card charge was made. Paid checkout, a paid Astra job through customer entitlements, narrated generation and annotation revisions were not re-tested in this release; their newer production implementation was preserved.

## Rollback

Restore both services to app image `f297081`, `E2B_TEMPLATE_VERSION=f297081`, and `ORUNE_CODE_MODEL_THOROUGH=gpt-5.6-sol`; remove `ORUNE_CODE_MODEL_BALANCED`. Preserve all other environment values, secret references and live billing. Update local Terraform variables to match. Do not reverse database migrations or restore the database for an application rollback.
